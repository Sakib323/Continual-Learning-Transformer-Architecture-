# Pinned for reproducibility across rented instances.
#
# Different GPUs already introduce numeric differences that show up in results
# looking like a mechanism effect; a drifting torch version on top of that makes
# a sweep uncomparable with the one before it. Bump deliberately, and re-run the
# controls when you do.
torch>=2.1,<3.0
pyyaml>=6.0
pytest>=7.4
